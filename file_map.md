# 🗺️ פרויקט Multi-Agent LeetCode Solver - מיפוי קבצים

מסמך זה מפרט את המבנה של המערכת, התפקיד של כל קובץ, והסיבה לקיומו בארכיטקטורת המערכת.

---

## 📂 תיקיית השורש (Root)
* **`README.md`**: המדריך הראשי של הפרויקט עבור מפתחים חיצוניים (התקנה, הרצה, אבטחה וטכנולוגיות).
* **`file_map.md`**: קובץ זה (מפת דרכים פנימית של קבצי הקוד).
* **`architecture.md`**: הבלופרינט הארכיטקטוני המקורי של מערכת הסוכנים.
* **`requirements.txt`**: רשימת כל חבילות צד-שלישי הנדרשות להרצת הפרויקט (LangGraph, Streamlit וכו').
* **`.env.example`**: תבנית ריקה המציגה אילו משתני סביבה (כמו מפתחות API) יש להגדיר מקומית.

---

## 📂 תיקיית ההגדרות (`config/`)
* **`config/settings.py`**: מנהל ההגדרות המרכזי. הוא טוען את קובץ ה-`.env` בצורה מאובטחת ומחזיק קבועים גלובליים כמו שמות המודלים וזמני הגבלת ריצה (Timeouts).

---

## 📂 תיקיית המקור הראשית (`src/`)

### 🔹 סוכנים ואורקסטרציה (`src/agents/`)
* **`agents/supervisor.py`**: סוכן הנתב (Router/Supervisor). הוא המוח המרכזי שמקבל את החלטות הניתוב בגרף וקובע איזה סוכן ייקרא לפעולה בכל שלב.
* **`agents/solver.py`**: מהנדס הקוד. הוא מקבל את השאלה ואת השפה המבוקשת, ומייצר שתי גרסאות של פתרון: נאיבי (כוח גס) ואופטימלי (Absolute Theoretical Limit).
* **`agents/performance.py`**: האנליסט האלגוריתמי. מנתח סטטית את סיבוכיות הזמן והמקום (Big-O Notation) של הפתרונות ומספק טרייד-אופים.
* **`agents/tester.py`**: מתאם הבדיקות. אחראי להחליט האם להריץ את הקוד פיזית ב-Sandbox (עבור פייתון) או לפנות לאימות לוגי-סמנטי מבוסס LLM (עבור שפות אחרות).
* **`agents/documenter.py`**: הכתב הטכני. אוסף את כל המידע שנצבר ב-State ומנסח דו"ח Markdown עשיר הכולל הסברים עמוקים, אינטואיציה אלגוריתמית וטיפים הנדסיים.

### 🔹 מבני נתונים וסכמות (`src/schemas/`)
* **`schemas/messages.py`**: מגדיר את ה-State המרכזי של LangGraph. זהו המבנה שמחזיק את המידע הדינמי (השאלה, השפה, הקודים, הטסטים והדו"ח) שעובר בין הסוכנים.
* **`schemas/solution.py`**: סכמת Pydantic המגדירה את המבנה המצופה מה-Solver (הפרדה נקייה בין הקוד הנאיבי לאופטימלי).
* **`schemas/complexity.py`**: סכמת Pydantic המכריחה את סוכן הביצועים להחזיר ניתוח Big-O מובנה (הסבר, זמן, מקום).
* **`schemas/test_case.py`**: סכמת Pydantic המגדירה את הפורמט של תוצאות הטסטים (קלט, פלט צפוי, פלט בפועל והאם עבר).

### 🔹 סביבת הרצה בטוחה (`src/sandbox/`)
* **`sandbox/executor.py`**: מנהל תהליכי הריצה הפיזיים. פותח תהליך מבודד (`subprocess`) ומריץ קוד פייתון עם הגנת גארדרילס קשיחה של 5 שניות למניעת לולאות אינסופיות.
* **`sandbox/test_generator.py`**: רכיב שמייצר קלטים מגוונים (cases רגילים, מקרי קצה ומקרי קיצון) כדי לאתגר את נכונות הקוד.

### 🔹 ליבת המערכת וממשק (`src/core/` & Main)
* **`core/graph.py`**: קובץ הבנייה של הגרף. מחבר את כל הסוכנים לצמתים (Nodes), מגדיר את קצוות הניתוב (Conditional Edges), ומקמפל את ה-Workflow של LangGraph.
* **`app.py`**: ממשק המשתמש הויזואלי (Frontend) שנבנה ב-Streamlit. מנהל את האינפוטים, כפתור הריצה, תצוגת הספינרים, והחלוקה של התוצרים הסופיים לכרטיסיות (Tabs).
* **`main.py`**: נקודת הכניסה של ה-Backend למקרה שרוצים להריץ את המערכת ישירות מהטרמינל (CLI) ללא דפדפן.